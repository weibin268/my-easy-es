server:
  port: 8808

spring:
  profiles:
    active: dev
  application:
    name: my-easy-es

logging:
  level:
    com.zhuang.easyes: debug

easy-es:
  enable: true # 默认为true,若为false时,则认为不启用本框架
  address: 192.168.3.186:9200  #填你的es连接地址
  # username: 有设置才填写,非必须
  # password: 有设置才填写,非必须
